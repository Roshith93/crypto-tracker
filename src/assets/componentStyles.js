import { makeStyles } from '@mui/styles'

export const useStyles = makeStyles({
  root: {
    backgroundColor: '#141618',
    color: 'white',
    minHeight: '100vh',
  },
  navBarTitle: {
    flex: 1,
    color: 'gold',
    fontWeight: 'bold',
    cursor: 'pointer',
  },
})
